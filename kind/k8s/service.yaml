apiVersion: v1
kind: Service
metadata:
  name: echo-service
  labels:
    app: echo-pod
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: echo-pod
