apiVersion: v1
kind: Service
metadata:
  name: tc10-service
  namespace: sigma
  labels:
    app: tc10-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: tc10-pod
