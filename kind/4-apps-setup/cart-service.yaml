# App Server for the Microservice
# Service Definition
apiVersion: v1
kind: Service
metadata:
  # Service Name: Ex. Order Service
  name: cart-service
  namespace: lambda
  labels:
    app: cart-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 9090
    protocol: TCP
    name: http
  selector:
    # Instance Labels
    # Ex. Order Service Instances
    app: cart-pod
    release: stable
    organization: Apache
